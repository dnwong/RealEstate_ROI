# Optional: override default user agent (match your Chrome version if blocked)
# ZILLOW_USER_AGENT=Mozilla/5.0 ...

# Set to 0 to never try Google Chrome channel (use bundled Chromium only)
# ZILLOW_USE_CHROME=0
